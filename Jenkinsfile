pipeline {
    agent any

    environment {
        def buildDestination = '/data/app/lelo/lelo-client'
        def workDirectory = '/var/lib/jenkins/workspace'
    }
    tools {
        go '1.19'
    }

    stages {
        stage ('Test') {
            steps {
                echo 'Test Skipped'
            }
        }
    }
}
